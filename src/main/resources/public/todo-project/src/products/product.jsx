import React from "react";

export default function Product(props) {
    console.log(props);
    return (
        <div>
            
            {/* <p>Products</p>
            {props.data.map((emp) => <Employee src={emp} />)} */}
        </div>
    );
}